deploy:
  provider: script
  script: echo "hello"


jobs:
  # include:
  #   - language: nodejs
  #     nodejs: 14
  #     before_install:
  #       - cd frontend
  #     install:
  #       - npm install
  #     script:
  #       - npm run build

    - langauge: java
      jdk: openjdk11
      before_install:
        - cd backend
        - chmod +x mvnw
        - mkdir src/main/resources
        - echo ${COVERALLS_REPO_TOKEN}
      script:
        - mvn test
