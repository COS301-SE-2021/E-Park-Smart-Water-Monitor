import img from '../../img/brand/login-background.jpg'

const componentStyles = (theme) => ({
  header: {
    // background:
    //   "linear-gradient(87deg," + theme.palette.info.main + ",#1171ef)",
    backgroundImage: `url(${img})`,
  },
});

export default componentStyles;
